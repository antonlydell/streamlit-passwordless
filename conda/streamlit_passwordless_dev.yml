name: streamlit_passwordless_dev
channels:
  - defaults
  - conda-forge
dependencies:
  # Run
  - python >=3.11
  - streamlit >=1.24

  # Test
  - pytest >=7.0

  # Build
  - python-build >=0.7
  - setuptools >=62.0
  - twine >=3.7

  # Tools
  - black >=22.0
  - flake8 >=4.0
  - mypy >=0.910
